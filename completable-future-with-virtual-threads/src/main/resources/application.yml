server.port: 8082
spring:
  application:
    name: completable-future-with-virtual-threads
  threads:
    virtual:
      enabled: true

spring-doc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /
    disable-swagger-default-url: true